version: "3.8"

services:
  siga:
    image: siga
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
    ports:
      - "3000:3000"
    restart: unless-stopped
    user: node
